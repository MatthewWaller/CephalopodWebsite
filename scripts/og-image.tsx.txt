import { ImageResponse } from "next/og"
import fs from "fs"
import path from "path"

export const dynamic = "force-static"

export const alt = "Cephalopod Studio — Bold. Vibrant. Playful."
export const size = { width: 1200, height: 630 }
export const contentType = "image/png"

export default async function OpengraphImage() {
  const octopus = fs.readFileSync(path.join(process.cwd(), "public/assets/octopus.png"))
  const octopusSrc = `data:image/png;base64,${octopus.toString("base64")}`

  return new ImageResponse(
    (
      <div
        style={{
          width: "100%",
          height: "100%",
          display: "flex",
          flexDirection: "column",
          alignItems: "center",
          justifyContent: "center",
          backgroundColor: "#0a0a0b",
          backgroundImage: "radial-gradient(circle, rgba(255,255,255,0.07) 3px, transparent 4px)",
          backgroundSize: "36px 36px",
          position: "relative",
        }}
      >
        {/* Memphis shapes, kept clear of the text */}
        <div
          style={{
            position: "absolute",
            left: -110,
            bottom: -110,
            width: 240,
            height: 240,
            borderRadius: 9999,
            backgroundColor: "#ff2e9a",
          }}
        />
        <div
          style={{
            position: "absolute",
            right: -70,
            top: -70,
            width: 190,
            height: 190,
            backgroundColor: "#ffc900",
            transform: "rotate(18deg)",
          }}
        />
        <div
          style={{
            position: "absolute",
            right: -60,
            bottom: -60,
            width: 170,
            height: 170,
            backgroundColor: "#14b8d4",
            transform: "rotate(45deg)",
          }}
        />
        <div
          style={{
            position: "absolute",
            left: -50,
            top: -50,
            width: 140,
            height: 140,
            backgroundColor: "#14b8d4",
            borderRadius: 9999,
            opacity: 0.35,
          }}
        />

        {/* Headline */}
        <div style={{ display: "flex", fontSize: 96, fontWeight: 800, letterSpacing: -2 }}>
          <span style={{ color: "#ff2e9a" }}>Bold.</span>
          <span style={{ color: "#14b8d4", marginLeft: 26 }}>Vibrant.</span>
          <span style={{ color: "#ffc900", marginLeft: 26 }}>Playful.</span>
        </div>

        {/* Tagline */}
        <div style={{ display: "flex", marginTop: 24, fontSize: 32, color: "#a1a1aa" }}>
          Software to empower and delight
        </div>

        {/* Brand */}
        <div style={{ display: "flex", alignItems: "center", marginTop: 48 }}>
          {/* eslint-disable-next-line @next/next/no-img-element */}
          <img src={octopusSrc} width={72} height={72} alt="" />
          <div style={{ display: "flex", fontSize: 44, fontWeight: 700, marginLeft: 20 }}>
            <span style={{ color: "#ff2e9a" }}>Cephalopod</span>
            <span style={{ color: "#fafafa", marginLeft: 12 }}>Studio</span>
          </div>
        </div>
      </div>
    ),
    { ...size }
  )
}
